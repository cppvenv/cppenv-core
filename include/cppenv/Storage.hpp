#pragma once

/**
 * @file Storage.hpp
 * @brief Convenience header — includes all storage types.
 * @author Nikola Radovanovic
 *
 * Include this header to pull in every storage class in one line.
 * Prefer including individual storage headers in headers that only
 * need one type — include this only in translation units that use
 * the full set.
 */

#include <cppenv/storage/Storage.hpp>
#include <cppenv/storage/LocalStorage.hpp>
